# Jedi Tasks - Приложение для управления задачами

Минималистичное приложение для управления задачами с хранением в Supabase и деплоем на Digital Ocean.

## Функции

- ✅ Создание и редактирование задач
- 📅 Установка даты выполнения (due_date)
- 🏷️ Теги для организации задач
- 📁 Различные view: Входящие, Сегодня, Без даты, Архив, Корзина
- 🗑️ Мягкое удаление с возможностью восстановления

## Технологии

- **Frontend:** React + TypeScript + Vite
- **Backend:** Supabase (PostgreSQL)
- **Hosting:** Digital Ocean App Platform

## Настройка

### 1. Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните SQL из `supabase/schema.sql` в SQL Editor
3. Скопируйте URL и anon key из Settings → API

### 2. Переменные окружения

```bash
cp .env.example .env
```

Заполните `.env`:
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 3. Запуск локально

```bash
npm install
npm run dev
```

### 4. Деплой на Digital Ocean

1. Запушьте код в GitHub
2. В Digital Ocean создайте App → выберите репозиторий
3. Добавьте переменные окружения:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. Тип: Static Site, команда сборки: `npm run build`, директория: `dist`

## Структура views

| View | Описание |
|------|----------|
| **Входящие (inbox)** | Задачи с датой выполнения, не завершены, не удалены |
| **Сегодня (today)** | Задачи на сегодня |
| **Без даты (unplaced)** | Задачи без due_date |
| **Архив (archived)** | Завершённые задачи |
| **Корзина (trash)** | Удалённые задачи |

## Структура БД

```sql
-- tasks: основная таблица задач
-- tags: теги
-- task_tags: связь многие-ко-многим
```

См. полную схему в `supabase/schema.sql`
